# 2026-08-14

## Worklist chat integration (finishing backlog item 4)
- `backend/worklist.py`: `sample_worklist` now computes SHAP `top_factors` per row (reusing `explain_prediction`), so every worklist patient carries the same explainability data the single-patient forms use.
- `frontend/src/Worklist.jsx`: added click-to-expand row detail — clicking a row opens a sticky side panel reusing `RiskMeter` + `ExplainChat` (same components as the forms), scoped to that patient's `top_factors`. Chat resets per row via a `key={JSON.stringify(selectedRow)}` on `ExplainChat`.
- Verified via curl (`/worklist/no-show?n=2`) and confirmed HMR compiled cleanly before calling it done.

## Calibration investigation (deferred fix, but confirmed the problem is real)
- User raised a good concern: could the risk % be mistaken for a literal chance of the outcome? Checked rather than guessed.
- Built `notebooks/05_calibration_check.ipynb` — reproduces the exact held-out split each model's `train.py` used, plots `sklearn.calibration.calibration_curve` (reliability curve) for both models.
- **Result: both models are meaningfully overconfident.** No-show model: predicted ~73% actually resolves to ~41% real no-show rate; readmission model: predicted ~71% actually resolves to ~28% real readmission rate. Every bucket in both models runs hot — likely a side effect of `class_weight='balanced'` (fixes class imbalance, distorts probability calibration as a known trade-off).
- Decided plan (documented in `plan.md`'s "Known gap to address later"), deliberately deferred to the end of the backlog: (1) stop showing the raw % in the UI/chatbot in the meantime, (2) add `CalibratedClassifierCV` to both training scripts + retrain, (3) re-check with the same notebook to confirm predicted ≈ actual, (4) only then decide whether to bring the % back.

## Aggregate/ops summary view (backlog item 5, now done)
- New backend function `summarize_worklist()` in `backend/worklist.py`: samples 300 held-out patients (bigger, more stable sample than the 20-row worklist table), returns flagged count/%, average risk, Low/Moderate/High band counts (same band logic as `RiskMeter`'s `getStatus`), and the most frequent SHAP top-factors across the sample (tallied by how often each feature appears in a patient's top-3 drivers).
- New endpoints: `GET /summary/no-show`, `GET /summary/readmission`.
- New frontend page `frontend/src/Overview.jsx` (+ `Overview.css`), added as a new "Overview" nav item and made the default landing view. Two stat tiles (flagged count, average risk) + two bar charts per model (risk distribution using the existing status-color tokens; top factors using the accent color) — invoked the `dataviz` skill before building since these are real charts, reused the app's already-validated palette rather than introducing new colors.
- User confirmed it looks good after checking in the browser.

## Alerts/persistence (backlog item 6, now done — closes the feature backlog)
- New `backend/alerts_db.py`: plain `sqlite3` (no ORM — one table doesn't need one), `backend/alerts.db` (gitignored). `init_db()`, `create_alert()`, `list_alerts(status=None)`, `update_alert_status()`.
- `POST /predict/no-show` and `POST /predict/readmission` now call `create_alert()` whenever a prediction is flagged — only single-patient form submissions persist (worklist/Overview samples are re-drawn demo data, not real decision points, so deliberately not saved).
- New endpoints: `GET /alerts?status=...`, `PATCH /alerts/{id}` (moves `new → acknowledged → resolved`, no other side effects — verified with the user this is intentional, just a status flip, nothing notifies/deletes).
- New frontend page `frontend/src/Alerts.jsx` (+ `.css`): status filter tabs, one card per alert with risk %, key patient fields, timestamp, and an action button walking the status forward.
- Verified end-to-end over curl before building the frontend: flagged submission creates a row, non-flagged doesn't, status updates work, bad IDs 404.
- **All 6 dashboard feature backlog items are now done.**

## Calibration fix — started (see plan.md "Calibration fix — in progress" for full detail)
Circled back to the deferred calibration gap. Working through it as a deliberate multi-step re-verification, not a one-line retrain:
1. **UI/chatbot fixed first.** `RiskMeter.jsx` no longer shows a raw numeric % — hero is now a status-colored band label ("Low/Moderate/High risk") + proportional bar. User picked the exact mockup via preview before implementing (band-label-only, not a smaller tweak). `backend/main.py`'s `/explain-chat` system prompt no longer quotes `risk_score`; new `get_risk_band()` helper mirrors `RiskMeter`'s threshold logic, and the prompt now explicitly refuses to give an exact percentage even if a user asks directly — verified live with a direct test.
2. **`CalibratedClassifierCV` added to both training scripts, retrained.** Discussed with the user first why this isn't a one-line swap: calibration is monotonic (preserves ranking, shifts the number scale), so the old thresholds (0.4/0.6) would silently flag a very different population if reused as-is — demonstrated this concretely by evaluating the calibrated model at the *old* threshold: no-show recall dropped from 92% (raw) to 12% (calibrated, same 0.4 cutoff); readmission recall dropped to **0%** at the old 0.6 cutoff. Also flagged upfront (before writing code) that `shap.TreeExplainer` can't run on a `CalibratedClassifierCV`-wrapped model, so SHAP explanations stay on the raw base model — a deliberate, documented decision, not an oversight (calibration doesn't change *why*, only rescales the final number; the raw contribution values were never shown to users anyway).
   - Implementation: `models/*/train.py` now splits data 60% fit / 20% calibrate / 20% test (test set identical to before — reproducibility for worklist/notebook preserved). Fits the raw model on the fit slice, calibrates with `method="sigmoid"` on the calibrate slice via `CalibratedClassifierCV(FrozenEstimator(model), ...)` — checked the installed sklearn version (1.9.0) first since `cv="prefit"` has been replaced by `FrozenEstimator` in recent sklearn, didn't guess from memory. Saves `calibrated_v1.joblib` alongside the existing `gradient_boosting_v1.joblib`.
3. **Re-verified with an updated `notebooks/05_calibration_check.ipynb`** (raw vs. calibrated side-by-side plots + Brier scores for both models). Confirmed calibration worked: worst-bucket gap shrank from +0.337/+0.430 (raw) to ±0.018/±0.009 (calibrated); Brier score improved 32% (no-show), 56% (readmission).
4. **Threshold re-derivation — discussed method with the user before computing anything.** User asked directly whether recall/precision was still the right lens now that scores are calibrated — walked through why calibration doesn't invalidate that (rank-preserving), but *does* newly unlock a more principled cost-based/expected-value approach. Worked the textbook formula (`threshold = cost_of_intervention / cost_of_missed_outcome`) with illustrative cost assumptions for both models — both came out near-zero (~0.007 no-show, ~0.005 readmission), which for readmission actually **contradicted** the original "favor precision" framing, because it doesn't account for a care coordinator's outreach capacity being finite (unlike a free-to-scale SMS reminder). Surfaced that gap explicitly rather than quietly picking a number. User chose: no-show gets a deliberately capped, less-extreme threshold rather than the raw cost-math answer; readmission gets a capacity-based cutoff (top ~10% riskiest patients). Generated real threshold-sweep tables from the actual calibrated held-out scores so the user picked from real numbers, not the abstract math alone. **Landed on 0.20 / 0.20** (no-show: ~55% flagged, 83% recall; readmission: ~10.8% flagged, recall 0.251, precision 0.265) — not comparable to the old raw-scale 0.4/0.6.
5. Documented the full decision trail in `plan.md`.

## Calibration fix — steps 5 & 6, done
- **Wired the calibrated model into the backend.** `backend/worklist.py`'s `WorklistSource` now holds `score_model` (calibrated — used for `predict_proba`/threshold) and `explain_model` (raw — used for SHAP) as two separate objects, not one. `backend/main.py` loads both `gradient_boosting_v1.joblib` and `calibrated_v1.joblib` per prediction type, updated `NO_SHOW_THRESHOLD`/`READMISSION_THRESHOLD` to 0.20/0.20, and wired scoring vs. explaining to the correct model in both `/predict/*` endpoints and the worklist sources.
- Re-verified end-to-end over curl before calling it done: single-patient predictions now show honest calibrated scores (a 25-day-wait patient dropped from the old raw 74% to a calibrated 44%, still correctly flagged); alerts still fire correctly and old pre-retrain alerts coexist fine as historical records; worklist rows score/explain correctly; Overview aggregates landed right where the threshold-sweep tables predicted (no-show ~62.5% flagged vs. ~55% target, readmission ~8.5% vs. ~10% target — sample variance, both directionally right).
- **Decided: keep the band-only RiskMeter UI, don't bring the numeric % back**, even though it would now be honest — simpler for staff to act on.
- **Calibration fix is fully done**, plan.md updated to close it out.

## Population-mismatch disclosure — deprioritized
User decided this isn't worth doing right now. Updated `plan.md`'s "Known gap" entry to reflect that explicitly (kept the reasoning documented in case it matters later, but it's not active work).

## UI polish pass
User asked for the UI to look more professional. Screenshotted the live app first (installed Playwright + headless Chromium in `.venv`, no display available in this sandbox) rather than guessing at what needed fixing — captured every page before touching anything.

**Research first**: searched for 2026 healthcare-dashboard and SaaS-dashboard UI guidance before adding color, since "add more color" and "clinical dashboards should stay restrained" are in tension. Synthesis: lean harder into the existing accent blue (already-validated palette) for interactive/summary elements, keep status colors (green/amber/red) strictly reserved for actual risk meaning — not decorative. This matched the dataviz skill's existing non-negotiables, so no new palette was introduced.

**Found and fixed a real bug while reviewing the code for this**: `frontend/src/App.jsx`, `NoShowForm.jsx`, and `ReadmissionForm.jsx` still hardcoded the *old* raw-scale thresholds (0.4 / 0.6) for the `RiskMeter` band display, even though the backend was already scoring on the calibrated scale with 0.20/0.20 thresholds (see calibration fix above). This meant the on-screen risk band could disagree with the backend's actual `flagged_high_risk` decision — e.g. a patient the backend flags as high-risk at 44% would render as "Moderate" client-side. Fixed all three to 0.20, with a comment tying them back to `backend/main.py`'s constants so they don't drift again. Verified live: the same wait_days=25 test case that used to show "Moderate risk" now correctly shows "High risk," matching the backend.

**Changes**:
- New design tokens (`index.css`): `--shadow-sm`/`--shadow-md` (card elevation, light+dark variants), `--accent-strong` (hover state).
- All card surfaces (`form-card`, `risk-meter-card`, `overview-section`, `alert-card`, `worklist-table-wrapper`) got subtle shadows instead of flat 1px borders.
- Sidebar: added hand-drawn inline SVG icons (`Icons.jsx`, six icons, no library dependency) per nav item; widened slightly; active/hover states now affect icon opacity too.
- Form inputs/selects: real focus rings (box-shadow instead of bare outline), custom cross-browser select chevron (replacing inconsistent native dropdown arrows), hover states.
- Empty-state result panel (forms): went from a plain dashed box with a sentence to a proper card with an icon, matching the form card's visual weight instead of floating in empty space.
- Buttons (submit, alert actions, worklist refresh, alert filter tabs): hover states with color shift + slight lift, not just flat/static.
- Overview stat tiles: numbers now colored by meaning (flagged count → status-critical red, average risk → accent blue) — a restrained, purposeful use of color per the research, not decoration.
- Worklist table: fixed the readmission table's columns silently running off the viewport edge with no visual cue — added a pure-CSS scroll-shadow effect (gradient that appears on whichever edge still has hidden columns, fades out once scrolled there) so it's now visually obvious there's more to scroll to.
- Verified via headless Chromium screenshots (before/after) and a full console-error sweep across every page — zero errors, zero visual regressions.

## Status
All 6 dashboard feature backlog items done. **Calibration fix fully done** (all 6 steps: UI fix, retrain+calibrate, re-verify, re-derive thresholds, wire into backend, decide on % display) -- including a frontend threshold bug caught and fixed during today's UI polish pass. UI polish pass done (cards, inputs, sidebar icons, buttons, restrained color, worklist scroll affordance). No open work remains on this project as of today.
